<div class="form-group">
	<?php
	$options = "";
	foreach ($this->element->getAttribs() as $name => $value):
		switch ($name):
			case 'checked':
				if ($value) {
					$options .=' ' . $name . "='" . $value . "'";
				}
				break;
			case 'options':
				break;
			default:
				$options .=' ' . $name . "='" . $value . "'";
				break;
		endswitch;
	endforeach;
	?>
	<div class="checkbox">
		<label>
			<input type="checkbox" value="<?php echo $this->element->getCheckedValue(); ?>" id="<?php echo $this->element->getId() ?>" name="<?php echo $this->element->getName() ?>" <?php echo $options; ?> /> <?php echo $this->element->getLabel() ?>
		</label>
	</div>
</div>